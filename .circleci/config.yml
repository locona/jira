version: 2

defaults: &defaults
  working_directory: ~/app
  docker:
    - image: circleci/node:12

jobs:
  release:
    <<: *defaults
    steps:
      - checkout
      - run: npx semantic-release

workflows:
  version: 2
  test_and_release:
    jobs:
      - release:
          filters:
            branches:
              only: master
