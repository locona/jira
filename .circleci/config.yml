release:
  <<: *defaults
  steps:
    - checkout
    - run: npm ci
    - run: npx semantic-release

workflows:
  version: 2
  test_and_release:
    jobs:
      - test
      - release:
          requires:
            - test
          filters:
            branches:
              only: master
